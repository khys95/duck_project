
  
  create view "analytics"."main"."inter_locations__dbt_tmp" as (
    /*
 SELECT index AS location_fk, category, status, amount, ship_city, ship_state, ship_postal_code, ship_country
FROM "analytics"."main"."stg_orders"

 */
  );
