
  
  create view "analytics"."main"."inter_products__dbt_tmp" as (
    /*
 SELECT index, order_id, style, sku, category, size, asin, qty AS quantity, amount,
FROM "analytics"."main"."stg_orders"

 */
  );
